#include <stdio.h>

int main(){
	int n,i,j,k,a[3000];
	int change;
	while ( scanf("%d",&n)==1 ){
		for (i=1;i<=n;i++) scanf("%d",&a[i]);
		change=1
		while (change ==1){
			change=0;
			for (i=1;i<=n;i++){
				for (j=1;j<=n;j++){
					for (k=1;k<=n;k++){
						if (
					}
				}
			}
		}

	}
	return 0;
}